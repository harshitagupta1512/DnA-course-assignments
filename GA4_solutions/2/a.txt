SELECT DOC_ID, freq/sum_words AS score FROM
(SELECT DOC_ID as ADOC_ID, sum(FREQ) AS sum_words FROM INVINDEX GROUP BY ADOC_ID) AS A 
INNER JOIN
(SELECT DOC_ID, sum(FREQ) AS freq FROM INVINDEX WHERE WORD="Hello" OR WORD="World" GROUP BY DOC_ID) AS B 
ON A.ADOC_ID=B.DOC_ID;
